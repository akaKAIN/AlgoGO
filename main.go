package main

import (
	"os"
)

/*
Выборка первых элементов файлов лежащих в указаном каталоге.
Каналы
Паралеллизм
Чтение файлов
Интерфейсы?
 */

const sepr  = os.PathSeparator


func main() {
	//var userDir, path string
	//var err error
	//if _, err := fmt.Scanln(&userDir); err != nil && userDir !="" {
	//	log.Printf("Error input: %s. Err:%s", userDir, err)
	//	os.Exit(1)
	//}
	//
	//if userDir == "" {
	//	path, err = os.Getwd()
	//}else{
	//	 if err = os.Chdir(userDir); err != nil {
	//	 	log.Printf("Указан неверный путь: %s. Error: %s\n", userDir, err)
	//	 }
	//}
	//
	//
	//
	//fmt.Println(userDir)
	//dir, _ := os.Getwd()
	//fmt.Println(dir, path)


}
